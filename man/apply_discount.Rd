% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shopping_functions.R
\name{apply_discount}
\alias{apply_discount}
\title{Apply a Discount to a Price or Selected Items: apply_discount}
\usage{
apply_discount(price = NULL, discount = NULL, item_row = NULL)
}
\arguments{
\item{price}{Numeric. The original price (used only if `item_row` is not provided).}

\item{discount}{Numeric between 0 and 1. The discount percentage to apply (default 0.10).}

\item{item_row}{Data frame row(s) from `food` or `gifts` containing columns
`price` and `discount`. If provided, the function calculates the total and average discount.}
}
\value{
Character. A formatted string showing the price after discount, along with the original total.
}
\description{
Calculates the discounted price for either a manually entered price or one/multiple
rows from a data frame (supports the 'food' or 'gifts' data frames). Returns a
descriptive string showing the final price after discount.
}
\examples{
# Manual price and discount
apply_discount(price = 50, discount = 0.15)

# Single item from food menu
apply_discount(item_row = food[1, ])

# Multiple items from gifts
apply_discount(item_row = gifts[1:3, ])

}
