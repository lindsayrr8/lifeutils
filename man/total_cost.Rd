% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shopping_functions.R
\name{total_cost}
\alias{total_cost}
\title{Calculate Total Cost with Discount and Sales Tax: total_cost}
\usage{
total_cost(price = NULL, discount = NULL, tax = NULL, item_rows = NULL)
}
\arguments{
\item{price}{Numeric. The base price (used only if `item_rows` is not provided).}

\item{discount}{Numeric between 0 and 1. The discount percentage (default 0 if manual input and not provided).}

\item{tax}{Numeric between 0 and 1. The sales tax percentage (default 0 if manual input and not provided).}

\item{item_rows}{Data frame rows from `food` or `gifts` containing columns `price`, `discount`, and `tax`.
If provided, calculates the total across all rows and prints the items.}
}
\value{
Prints the total cost after discount and tax. (Truncated to 2 decimals.)
}
\description{
Computes the total cost for either a manually entered price or multiple rows from a data frame.
Supports the 'food' and 'gifts' sample data frames. Returns the total price after applying
discounts and taxes. If using a data frame, prints the rows being used; for manual input, prints
the values provided.
}
\examples{
# Manual calculation
total_cost(price = 50, discount = 0.1, tax = 0.08)

# Using multiple items from food menu
total_cost(item_rows = food[1:3, ])

# Using multiple items from gifts
total_cost(item_rows = gifts[1:2, ])

}
