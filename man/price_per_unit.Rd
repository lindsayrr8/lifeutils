% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/price_per_unit.R
\name{price_per_unit}
\alias{price_per_unit}
\title{Calculate Price Per Unit for Bulk Items: price_per_unit}
\usage{
price_per_unit(price = NULL, quantity = NULL, bulk_row = NULL)
}
\arguments{
\item{price}{Numeric. Total price of the bulk package (non-negative). Required if `bulk_row` is not used.}

\item{quantity}{Numeric. Number of items in the package (positive integer). Required if `bulk_row` is not used.}

\item{bulk_row}{Data frame. Optional single row from the `bulk` data frame containing columns: `item`, `package_price`, `quantity`.}
}
\value{
Prints the item details and price per unit. Does not return a value.
}
\description{
Computes the price per individual unit for bulk items. Supports either manual input
of `price` and `quantity` or a single row from the `bulk` data frame.
Prints the item details (from the data frame or manual input) and the per-unit price.
}
\examples{
# Using manual input
price_per_unit(price = 20, quantity = 4)

# Using a row from the bulk data frame
price_per_unit(bulk_row = bulk[1, ])

}
